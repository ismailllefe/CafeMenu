@model IEnumerable<CafeMenu.Models.Property>

@{
    ViewBag.Title = "Property";
}


<a href="@Url.Action("Create", "Property")" class="btn btn-primary">Add New Property</a>

<div class="row">
    <div class="col-lg-12">
        <div class="card-style mb-30">
            <div class="table-responsive">
                <table class="table top-selling-table">
                    <thead>
                        <tr>
                            <th>
                                <h6 class="text-sm text-medium">Key</h6>
                            </th>
                            <th>
                                <h6 class="text-sm text-medium">
                                    Value <i class="lni lni-arrows-vertical"></i>
                                </h6>
                            </th>
                            <th>
                                <h6 class="text-sm text-medium">
                                    Actions <i class="lni lni-arrows-vertical"></i>
                                </h6>
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    <div>
                                        <p class="text-sm"> @Html.DisplayFor(modelItem => item.Key)</p>
                                    </div>
                                </td>
                                <td>
                                    <p class="text-sm"> @Html.DisplayFor(model => item.Value)</p>
                                </td>
                                <td>
                                    <div class="action justify-content-end">
                                        <button class="more-btn ml-10 dropdown-toggle" id="moreAction1" data-bs-toggle="dropdown"
                                                aria-expanded="false">
                                            <i class="lni lni-more-alt"></i>
                                        </button>
                                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="moreAction1">
                                            <li class="dropdown-item">
                                                @using (Html.BeginForm("Delete", "Property", FormMethod.Post))
                                                {
                                                    @Html.Hidden("id", item.PropertyID)
                                                    <button type="submit" class="text-gray">Remove</button>
                                                }
                                            </li>
                                            <li class="dropdown-item">
                                                <a href="@Url.Action("Edit", "Property", new { id = item.PropertyID })" class="text-gray">Edit</a>
                                            </li>
                                        </ul>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
