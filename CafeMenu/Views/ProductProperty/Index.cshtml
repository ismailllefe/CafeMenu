@model IEnumerable<CafeMenu.Models.ProductProperty>


<a href="@Url.Action("Create", "ProductProperty")" class="btn btn-primary">Add New Product Property</a>

<div class="row">
    <div class="col-lg-12">
        <div class="card-style mb-30">
            <div class="table-responsive">
                <table class="table top-selling-table">
                    <thead>
                        <tr>
                            <th>
                                <h6 class="text-sm text-medium">Product Name</h6>
                            </th>
                            <th>
                                <h6 class="text-sm text-medium">
                                    Property Key 
                                </h6>
                            </th>
                            <th>
                                <h6 class="text-sm text-medium">
                                    Property Value <i class="lni lni-arrows-vertical"></i>
                                </h6>
                            </th>
                            <th>
                                <h6 class="text-sm text-medium">
                                    Actions <i class="lni lni-arrows-vertical"></i>
                                </h6>
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    <div>
                                        <p class="text-sm"> @Html.DisplayFor(modelItem => item.Product.ProductName)</p>
                                    </div>
                                </td>
                                <td>
                                    <p class="text-sm"> @Html.DisplayFor(model => item.Property.Key)</p>
                                </td>
                                <td>
                                    <p class="text-sm"> @Html.DisplayFor(model => item.Property.Value)</p>
                                </td>
                                <td>
                                    <div class="action justify-content-end">
                                        <button class="more-btn ml-10 dropdown-toggle" id="moreAction1" data-bs-toggle="dropdown"
                                                aria-expanded="false">
                                            <i class="lni lni-more-alt"></i>
                                        </button>
                                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="moreAction1">
                                            <li class="dropdown-item">
                                                @using (Html.BeginForm("Delete", "ProductProperty", FormMethod.Post))
                                                {
                                                    @Html.Hidden("id", item.ProductPropertyID)
                                                    <button type="submit" class="text-gray">Remove</button>
                                                }
                                            </li>
                                            <li class="dropdown-item">
                                                <a href="@Url.Action("Edit", "ProductProperty", new { id = item.ProductID })" class="text-gray">Edit</a>
                                            </li>
                                        </ul>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>